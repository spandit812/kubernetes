It ensures that a copy of a pod is available in every node of the cluster.

It is used to deploy system daemons, such as log connector, monitoring agents

ReplicaSet:
When your application is completely decoupled from the node.
DaemonSet:
When a sigle copy of application must run on all node
